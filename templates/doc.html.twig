{# ceci est un commentaire
sur plusieurs lignes
#}

{# correponds à un short echo tag: écrire la valeur de la variable tagada 
il n'y a pas de $
#}
{{ tagada }}

{#
tout ce qui va être instruction Twig sera avec des %
il n'y a pas de foreach, c'est le for :shrug:
#}
{# https://twig.symfony.com/doc/3.x/tags/if.html #}
{% if condition %}

{% endif %}
{# https://twig.symfony.com/doc/3.x/tags/for.html #}
{% for item in items %}
    
{% endfor %}