{% extends 'base_backoffice.html.twig' %}

{% block title %}Casting{% endblock %}

{% block body %}
    <h1>Casting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ casting.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ casting.role }}</td>
            </tr>
            <tr>
                <th>CreditOrder</th>
                <td>{{ casting.creditOrder }}</td>
            </tr>
        </tbody>
    </table>

    {{ include ('backoffice/links/back_to_list.html.twig', { 'custom_path' : path('app_backoffice_casting_index')})}}

    <a href="{{ path('app_backoffice_casting_edit', {'id': casting.id}) }}">edit</a>

    {{ include('backoffice/casting/_delete_form.html.twig') }}
{% endblock %}
