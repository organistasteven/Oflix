{% extends 'base_backoffice.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ genre.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_genre_index') }}">back to list</a>

    <a href="{{ path('app_backoffice_genre_edit', {'id': genre.id}) }}">edit</a>

    {{ include('backoffice/genre/_delete_form.html.twig') }}
{% endblock %}
